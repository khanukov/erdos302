name: Integration critical CI

on:
  push:
    branches: [main, agent/integration-ci]
  workflow_dispatch:
    inputs:
      published_run_id:
        description: "Verify-only mode: run containing a published artifact"
        required: false
        type: string
      published_artifact_sha:
        description: "Verify-only mode: exact commit named by the artifact"
        required: false
        type: string

permissions:
  actions: read
  contents: read

concurrency:
  group: integration-critical-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  STAGING_TAG: integration-overlay-20260907

jobs:
  semantic-stage:
    if: ${{ inputs.published_run_id == '' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: leanprover/lean-action@38fbc41a8c28c4cbaec22d7f7de508ec2e7c0dd9 # v1
        with:
          build: false
          use-github-cache: false
      - name: Report resources
        run: |
          nproc
          free -h
          df -h .
          lean --version
      - name: Fetch Mathlib cache
        run: lake exe cache get
      - name: Download verified integration overlay
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p staging-parts
          gh release download "$STAGING_TAG" --repo "$GITHUB_REPOSITORY" --pattern 'integration-overlay.part-*' --dir staging-parts
          python3 scripts/assemble_integration_overlay.py
          zstd -t integration-overlay.tar.zst
          zstd -d -q -c integration-overlay.tar.zst | tar -xf -
          python3 scripts/verify_integration_overlay.py
      - name: Regenerate bridge and require exact source
        run: |
          set -euo pipefail
          before=$(sha256sum Erdos302/Generated/PackingCertificateLevelsNat*.lean | sha256sum)
          semantic_before=$(sha256sum Erdos302/Generated/SemanticPackingChecks*.lean Erdos302/Generated/SemanticChecks.lean | sha256sum)
          python3 scripts/generate_packing_level_bridge.py
          python3 scripts/generate_semantic_packing_bridge.py
          after=$(sha256sum Erdos302/Generated/PackingCertificateLevelsNat*.lean | sha256sum)
          semantic_after=$(sha256sum Erdos302/Generated/SemanticPackingChecks*.lean Erdos302/Generated/SemanticChecks.lean | sha256sum)
          test "$before" = "$after"
          test "$semantic_before" = "$semantic_after"
      - name: Compile semantic stage with Lean kernel
        run: |
          set -euo pipefail
          compile() {
            src="$1"
            mod="${src%.lean}"
            mkdir -p ".lake/build/lib/lean/$(dirname "$mod")"
            /usr/bin/time -v lake env lean -s 1048576 "$src" \
              -o ".lake/build/lib/lean/${mod}.olean" \
              -i ".lake/build/lib/lean/${mod}.ilean" \
              2>&1 | tee "$(basename "$mod").log"
            test -s ".lake/build/lib/lean/${mod}.olean"
            test -s ".lake/build/lib/lean/${mod}.ilean"
          }
          compile Erdos302/Generated/PackingCertificateLevelsNat.lean
          compile Erdos302/Valuation.lean
          compile Erdos302/Asymptotic/PeriodicCount.lean
          compile Erdos302/Asymptotic/Multipliers.lean
          compile Erdos302/Asymptotic/FiniteOmission.lean
          compile Erdos302/Asymptotic/Thresholds.lean
          compile Erdos302/Asymptotic/ShiftSum.lean
          compile Erdos302/Asymptotic/Interface.lean
          compile Erdos302/SemanticBridge.lean
          compile Erdos302/Generated/SemanticPackingChecksCore.lean
          for i in $(seq 0 13); do
            compile "Erdos302/Generated/SemanticPackingChecksGroup${i}.lean"
          done
          compile Erdos302/Generated/SemanticChecks.lean
      - name: Pack semantic stage outputs
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json,os
          from pathlib import Path
          from scripts.restore_semantic_stage import EXPECTED,digest
          entries=[]
          for name in sorted(EXPECTED):
              path=Path(name)
              if not path.is_file() or path.is_symlink():
                  raise SystemExit(f'missing semantic-stage output: {name}')
              entries.append({'path':name,'bytes':path.stat().st_size,'sha256':digest(path)})
          manifest={'schema':1,'commit':os.environ['GITHUB_SHA'],'files':entries}
          Path('SEMANTIC-STAGE-MANIFEST.json').write_text(json.dumps(manifest,indent=2)+'\n')
          PY
          tar -cf semantic-stage.tar \
            .lake/build/lib/lean/Erdos302/Generated/PackingCertificateLevelsNat.{o,i}lean \
            .lake/build/lib/lean/Erdos302/Valuation.{o,i}lean \
            .lake/build/lib/lean/Erdos302/Asymptotic/PeriodicCount.{o,i}lean \
            .lake/build/lib/lean/Erdos302/Asymptotic/Multipliers.{o,i}lean \
            .lake/build/lib/lean/Erdos302/Asymptotic/FiniteOmission.{o,i}lean \
            .lake/build/lib/lean/Erdos302/Asymptotic/Thresholds.{o,i}lean \
            .lake/build/lib/lean/Erdos302/Asymptotic/ShiftSum.{o,i}lean \
            .lake/build/lib/lean/Erdos302/Asymptotic/Interface.{o,i}lean \
            .lake/build/lib/lean/Erdos302/SemanticBridge.{o,i}lean \
            .lake/build/lib/lean/Erdos302/Generated/SemanticPackingChecks*.olean \
            .lake/build/lib/lean/Erdos302/Generated/SemanticPackingChecks*.ilean \
            .lake/build/lib/lean/Erdos302/Generated/SemanticChecks.{o,i}lean \
            SEMANTIC-STAGE-MANIFEST.json
          zstd -T0 -10 semantic-stage.tar -o semantic-stage.tar.zst
          zstd -t semantic-stage.tar.zst
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: semantic-stage-${{ github.sha }}
          path: semantic-stage.tar.zst
          if-no-files-found: error
          retention-days: 3

  downstream:
    needs: semantic-stage
    if: ${{ needs.semantic-stage.result == 'success' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 480
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: leanprover/lean-action@38fbc41a8c28c4cbaec22d7f7de508ec2e7c0dd9 # v1
        with:
          build: false
          use-github-cache: false
      - name: Fetch Mathlib cache
        run: lake exe cache get
      - name: Download verified integration overlay
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p staging-parts
          gh release download "$STAGING_TAG" --repo "$GITHUB_REPOSITORY" --pattern 'integration-overlay.part-*' --dir staging-parts
          python3 scripts/assemble_integration_overlay.py
          zstd -t integration-overlay.tar.zst
          zstd -d -q -c integration-overlay.tar.zst | tar -xf -
          python3 scripts/verify_integration_overlay.py
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: semantic-stage-${{ github.sha }}
          path: semantic-stage-download
          run-id: ${{ github.run_id }}
          github-token: ${{ github.token }}
      - name: Verify and restore semantic stage
        run: |
          set -euo pipefail
          zstd -t semantic-stage-download/semantic-stage.tar.zst
          zstd -dc semantic-stage-download/semantic-stage.tar.zst > semantic-stage.tar
          python3 scripts/restore_semantic_stage.py \
            --tar semantic-stage.tar \
            --destination . \
            --expected-sha "$GITHUB_SHA"
          test -s .lake/build/lib/lean/Erdos302/Generated/SemanticChecks.olean
      - name: Add bounded swap for combined proof closures
        run: |
          set -euo pipefail
          sudo fallocate -l 32G /swapfile-extra
          sudo chmod 600 /swapfile-extra
          sudo mkswap /swapfile-extra
          sudo swapon /swapfile-extra
          free -h
      - name: Reject project-local proof escapes and unresolved imports
        run: |
          python3 -I -S scripts/test_source_audits.py
          python3 scripts/audit_lean_sources.py
          python3 scripts/audit_project_imports.py
      - name: Regenerate configuration validity bridge
        run: |
          set -euo pipefail
          before=$(sha256sum Erdos302/Generated/ConfigurationValidity*.lean | sha256sum)
          python3 scripts/generate_configuration_validity_bridge.py
          after=$(sha256sum Erdos302/Generated/ConfigurationValidity*.lean | sha256sum)
          test "$before" = "$after"
      - name: Compile downstream critical path with Lean kernel
        run: |
          set -euo pipefail
          compile() {
            src="$1"
            mod="${src%.lean}"
            mkdir -p ".lake/build/lib/lean/$(dirname "$mod")"
            /usr/bin/time -v lake env lean -s 1048576 "$src" \
              -o ".lake/build/lib/lean/${mod}.olean" \
              -i ".lake/build/lib/lean/${mod}.ilean" \
              2>&1 | tee "$(basename "$mod").log"
            test -s ".lake/build/lib/lean/${mod}.olean"
            test -s ".lake/build/lib/lean/${mod}.ilean"
          }
          compile Erdos302/Generated/ConfigurationValidityCore.lean
          for i in $(seq 0 14); do
            compile "Erdos302/Generated/ConfigurationValidityGroup${i}.lean"
          done
          compile Erdos302/Generated/ConfigurationValidity.lean
          compile Erdos302/Packing/PrefixTransportCore.lean
          compile Erdos302/Packing/PrefixTransport.lean
          compile Erdos302/Packing/PrefixOmission.lean
          compile Erdos302/Asymptotic/Endpoint.lean
          compile Erdos302/Asymptotic/Integration.lean
          compile Erdos302/Asymptotic.lean

      - name: Fresh kernel replay of the complete imported environment
        env:
          LEAN4CHECKER_COMMIT: 7df74851c95d9bd1bbb8fc9b51aeb291304faaf6
        run: |
          set -euo pipefail
          checker="$RUNNER_TEMP/lean4checker"
          git clone --quiet --depth 1 --branch v4.27.0 \
            https://github.com/leanprover/lean4checker "$checker"
          test "$(git -C "$checker" rev-parse HEAD)" = "$LEAN4CHECKER_COMMIT"
          cp lean-toolchain "$checker/lean-toolchain"
          lake -d "$checker" build
          (cd "$checker" && lake test)
          : > Lean4CheckerFresh.log
          /usr/bin/time -v lake env \
            "$checker/.lake/build/bin/lean4checker" \
            --fresh Erdos302.Asymptotic.Integration \
            2>&1 | tee -a Lean4CheckerFresh.log
          echo FRESH_INTEGRATION_REPLAY_OK | tee -a Lean4CheckerFresh.log
          /usr/bin/time -v lake env \
            "$checker/.lake/build/bin/lean4checker" \
            --num-workers=1 Erdos302.Asymptotic \
            2>&1 | tee -a Lean4CheckerFresh.log
          echo FINAL_MODULE_REPLAY_OK | tee -a Lean4CheckerFresh.log
          grep -Fx 'FRESH_INTEGRATION_REPLAY_OK' Lean4CheckerFresh.log
          grep -Fx 'FINAL_MODULE_REPLAY_OK' Lean4CheckerFresh.log
      - name: Audit final theorem axioms
        run: |
          set -euo pipefail
          cat > AxiomAudit.lean <<'EOF'
          import Erdos302.Asymptotic
          #print axioms Erdos302.Generated.repeatedThresholdLevelAt
          #print axioms Erdos302.Packing.prefix_omission_certificate
          #print axioms Erdos302.Asymptotic.erdos302Certificate_holds
          #print axioms Erdos302.Asymptotic.erdos_302_upper_140803024_163562355
          #print axioms Erdos302.Asymptotic.f302_upper_140803024_163562355
          EOF
          lake env lean -s 1048576 AxiomAudit.lean 2>&1 | tee AxiomAudit.log
          python3 - <<'PY'
          import re
          from pathlib import Path
          text = Path('AxiomAudit.log').read_text()
          found = re.findall(r"depends on axioms: (\[[^\n]*\])", text)
          expected = '[propext, Classical.choice, Quot.sound]'
          if found != [expected] * 5:
              raise SystemExit(f'unexpected axiom audit: {found!r}')
          print('axiom_audit_green declarations=5 trusted=' + expected)
          PY
      - name: Build output manifest
        run: |
          python3 - <<'PY'
          import hashlib,json,subprocess
          from pathlib import Path
          stems=[
            'Erdos302/Generated/PackingCertificateLevelsNat',
            'Erdos302/Valuation',
            'Erdos302/Asymptotic/PeriodicCount',
            'Erdos302/Asymptotic/Multipliers',
            'Erdos302/Asymptotic/FiniteOmission',
            'Erdos302/Asymptotic/Thresholds',
            'Erdos302/Asymptotic/ShiftSum',
            'Erdos302/Asymptotic/Interface',
            'Erdos302/SemanticBridge',
            'Erdos302/Generated/SemanticPackingChecksCore',
            *[f'Erdos302/Generated/SemanticPackingChecksGroup{i}' for i in range(14)],
            'Erdos302/Generated/SemanticChecks',
            'Erdos302/Generated/ConfigurationValidityCore',
            *[f'Erdos302/Generated/ConfigurationValidityGroup{i}' for i in range(15)],
            'Erdos302/Generated/ConfigurationValidity',
            'Erdos302/Packing/PrefixTransportCore',
            'Erdos302/Packing/PrefixTransport',
            'Erdos302/Packing/PrefixOmission',
            'Erdos302/Asymptotic/Endpoint',
            'Erdos302/Asymptotic/Integration',
            'Erdos302/Asymptotic',

          ]
          files=[Path('.lake/build/lib/lean')/(s+ext) for s in stems for ext in ('.olean','.ilean')]
          def h(p): return hashlib.sha256(p.read_bytes()).hexdigest()
          metadata=[Path('lean-toolchain'),Path('lakefile.toml'),Path('lake-manifest.json'),Path('.github/workflows/integration-critical-ci.yml')]
          m={
            'schema':2,
            'commit':subprocess.check_output(['git','rev-parse','HEAD'],text=True).strip(),
            'tree':subprocess.check_output(['git','rev-parse','HEAD^{tree}'],text=True).strip(),
            'lean':subprocess.check_output(['lean','--version'],text=True).splitlines()[0],
            'overlay':{'bytes':2972900109,'sha256':'c0dfb31167c18787dd3352f728cd1aa44776697ce9263a8294c3ff72d186c05b'},
            'axiom_audit_sha256':h(Path('AxiomAudit.log')),
            'lean4checker_fresh_sha256':h(Path('Lean4CheckerFresh.log')),
            'metadata':[{'path':str(p),'bytes':p.stat().st_size,'sha256':h(p)} for p in metadata],
            'files':[{'path':str(p),'bytes':p.stat().st_size,'sha256':h(p)} for p in files],
          }
          Path('INTEGRATION-MANIFEST.json').write_text(json.dumps(m,indent=2)+'\n')
          PY
      - name: Pack publication artifact
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from pathlib import Path
          manifest=json.loads(Path('INTEGRATION-MANIFEST.json').read_text())
          paths=[entry['path'] for entry in manifest['files']]
          paths += ['INTEGRATION-MANIFEST.json', 'AxiomAudit.log', 'Lean4CheckerFresh.log']
          Path('publication-files.txt').write_text('\n'.join(dict.fromkeys(paths))+'\n')
          PY
          tar -cf integration-critical.tar -T publication-files.txt
          zstd -T0 -10 integration-critical.tar -o integration-critical.tar.zst
          zstd -t integration-critical.tar.zst
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: integration-critical-${{ github.sha }}
          path: integration-critical.tar.zst
          if-no-files-found: error
          retention-days: 90

  verify-artifact:
    needs: downstream
    if: ${{ always() && (needs.downstream.result == 'success' || inputs.published_run_id != '') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: 54941f1a83dc66e359edf4490f9ef8e0ea4e426a
          path: verifier-source
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.published_artifact_sha || github.sha }}
          path: proof-source
      - name: Verify published source-run provenance
        if: ${{ inputs.published_run_id != '' }}
        env:
          GH_TOKEN: ${{ github.token }}
          SOURCE_RUN_ID: ${{ inputs.published_run_id }}
          ARTIFACT_SHA: ${{ inputs.published_artifact_sha }}
        run: |
          set -euo pipefail
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$SOURCE_RUN_ID" > source-run.json
          python3 verifier-source/scripts/verify_github_run_provenance.py \
            --run-json source-run.json \
            --repository "$GITHUB_REPOSITORY" \
            --expected-sha "$ARTIFACT_SHA" \
            --workflow-file proof-source/.github/workflows/integration-critical-ci.yml
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: integration-critical-${{ inputs.published_artifact_sha || github.sha }}
          path: verified-download
          run-id: ${{ inputs.published_run_id || github.run_id }}
          github-token: ${{ github.token }}
      - name: Verify complete published artifact
        working-directory: proof-source
        run: |
          set -euo pipefail
          zstd -t ../verified-download/integration-critical.tar.zst
          zstd -dc ../verified-download/integration-critical.tar.zst > ../verified-download/integration-critical.tar
          python3 ../verifier-source/scripts/verify_published_integration_artifact.py \
            --tar ../verified-download/integration-critical.tar \
            --extract-dir ../verified-download/extracted \
            --expected-sha "${PUBLISHED_ARTIFACT_SHA:-$GITHUB_SHA}"
        env:
          PUBLISHED_ARTIFACT_SHA: ${{ inputs.published_artifact_sha }}
